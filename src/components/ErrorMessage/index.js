import styled from 'styled-components';

const ErrorMessage = styled.div`
  width:inherit;
  padding:20px;
  background: #ef9a9a;
  font-size:1.1rem;
  margin: 10px 0;
`;

export default ErrorMessage
